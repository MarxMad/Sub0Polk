# Arkiv Network Configuration
ARKIV_PRIVATE_KEY=0x...  # Generate: node -e "console.log('0x' + require('crypto').randomBytes(32).toString('hex'))"

# Polkadot Configuration
NEXT_PUBLIC_POLKADOT_CONTRACT_ADDRESS=5...  # Deployed contract address
NEXT_PUBLIC_POLKADOT_RPC=wss://testnet-passet-hub.polkadot.io

# Base Configuration
NEXT_PUBLIC_BASE_CONTRACT_ADDRESS=0x...  # Deployed contract address
